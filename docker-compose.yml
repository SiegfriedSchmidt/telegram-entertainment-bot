services:
  tg-ssh-bot:
    platform: linux/amd64
    build: .
    image: siegfriedschmidt/telegram-entertainment-bot
    container_name: telegram-entertainment-bot
    environment:
      - TZ=Europe/Moscow
    volumes:
      - ./secret:/app/secret
      - ./data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"

    restart: "no"
